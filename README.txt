All the code and website layout was generated by OpenAI ChatGPT.

PROMPTS that generated the code

======================================================================================================
Ganesh Budhathoki
css code to shadow a box and make the box transparent
To add a shadow to a box and make it transparent, you can use the following CSS code:

Copy code
.box {
    box-shadow: 10px 10px 5px 0px rgba(0,0,0,0.75);
    opacity: 0.5;
}
This will add a shadow to the box with an offset of 10px on the x-axis and 10px on the y-axis, a blur radius of 5px, and an RGBA color value of (0, 0, 0, 0.75) for the black color with 75% opacity. The opacity property is set to 0.5, making the box transparent.

You can adjust the values of box-shadow, opacity according to your design.




Ganesh Budhathoki
basic index html code to start with
The basic index.html code to start with is as follows:

Copy code
<!DOCTYPE html>
<html>
<head>
  <title>My website</title>
</head>
<body>
  <h1>Welcome to my website!</h1>
  <p>This is a sample website.</p>
</body>
</html>
This code creates an HTML5 document and includes the basic structure of an HTML document, including the doctype, <html>, <head>, <title>, <body>, <h1>, and <p> elements.

The <!DOCTYPE> declaration defines the document type and version of HTML used in the document.

The <html> element is the root element of an HTML page.

The <head> element contains meta information about the document, such as the title of the page, which appears in the browser's title bar or tab.

The <title> element specifies the title of the document, which is displayed in the browser's title bar.

The <body> element contains the visible content of the document.

The <h1> and <p> elements are used to add headings and paragraphs of text to the website.

You can add more elements and styles to this basic structure to create a more complex and functional website.




Ganesh Budhathoki
code to reference a css file name main.css
To reference a CSS file named "main.css" in an HTML document, you need to use the link element within the head section of the HTML document. The link element should have a rel attribute set to "stylesheet" and a href attribute set to the path of the CSS file. Here is an example:

Copy code
<head>
  <title>My website</title>
  <link rel="stylesheet" href="main.css">
</head>
This code assumes that the "main.css" file is located in the same directory as the HTML document. If the CSS file is located in a different directory, you'll need to provide the correct path to the file in the href attribute.

It's also possible to include multiple CSS files in an HTML document by adding multiple link elements, each with its own href attribute pointing to a different CSS file.

Also, you can use the "@import" rule inside your main.css file to import other CSS files.

Copy code
@import url('path/to/other.css');
This can be useful when you want to keep your CSS organized in multiple files.




Ganesh Budhathoki
code to display square image of name hello.jpg
To display a square image named "hello.jpg" in an HTML document, you can use the img element. Here is an example:

Copy code
<img src="hello.jpg" alt="Hello" width="100" height="100">
This code assumes that the "hello.jpg" file is located in the same directory as the HTML document. If the image is located in a different directory, you'll need to provide the correct path to the file in the src attribute.

The alt attribute provides a text description of the image, which is displayed if the image cannot be loaded.

The width and height attributes are used to specify the width and height of the image. In this case, the image will be displayed as 100 pixels wide and 100 pixels high.

It's also possible to add CSS styles to the image using the class or id attribute, and then styling it in the CSS file.

Copy code
<img src="hello.jpg" alt="Hello" class="square-image">
Copy code
.square-image{
    width:100px;
    height:100px;
}
You can use the object-fit and object-position css property to control how the image is displayed inside the container.

Copy code
.square-image{
    width:100px;
    height:100px;
    object-fit: cover;
    object-position: center;
}
This will make the image cover the entire container, and center it if the aspect ratio of the image is different from the container.